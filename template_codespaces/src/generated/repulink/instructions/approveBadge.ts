/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getOptionDecoder,
  getOptionEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { REPULINK_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const APPROVE_BADGE_DISCRIMINATOR = new Uint8Array([
  178, 187, 48, 68, 17, 48, 184, 172,
]);

export function getApproveBadgeDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    APPROVE_BADGE_DISCRIMINATOR,
  );
}

export type ApproveBadgeInstruction<
  TProgram extends string = typeof REPULINK_PROGRAM_ADDRESS,
  TAccountReviewer extends string | AccountMeta<string> = string,
  TAccountFreelancer extends string | AccountMeta<string> = string,
  TAccountBadge extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountReviewer extends string
        ? WritableSignerAccount<TAccountReviewer> &
            AccountSignerMeta<TAccountReviewer>
        : TAccountReviewer,
      TAccountFreelancer extends string
        ? ReadonlyAccount<TAccountFreelancer>
        : TAccountFreelancer,
      TAccountBadge extends string
        ? WritableAccount<TAccountBadge>
        : TAccountBadge,
      ...TRemainingAccounts,
    ]
  >;

export type ApproveBadgeInstructionData = {
  discriminator: ReadonlyUint8Array;
  badgeIndex: number;
  clientLinkedin: Option<string>;
  clientTwitter: Option<string>;
  clientEmailReviewer: Option<string>;
};

export type ApproveBadgeInstructionDataArgs = {
  badgeIndex: number;
  clientLinkedin: OptionOrNullable<string>;
  clientTwitter: OptionOrNullable<string>;
  clientEmailReviewer: OptionOrNullable<string>;
};

export function getApproveBadgeInstructionDataEncoder(): Encoder<ApproveBadgeInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["badgeIndex", getU32Encoder()],
      [
        "clientLinkedin",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      [
        "clientTwitter",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
      [
        "clientEmailReviewer",
        getOptionEncoder(
          addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
        ),
      ],
    ]),
    (value) => ({ ...value, discriminator: APPROVE_BADGE_DISCRIMINATOR }),
  );
}

export function getApproveBadgeInstructionDataDecoder(): Decoder<ApproveBadgeInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["badgeIndex", getU32Decoder()],
    [
      "clientLinkedin",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    [
      "clientTwitter",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
    [
      "clientEmailReviewer",
      getOptionDecoder(addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())),
    ],
  ]);
}

export function getApproveBadgeInstructionDataCodec(): Codec<
  ApproveBadgeInstructionDataArgs,
  ApproveBadgeInstructionData
> {
  return combineCodec(
    getApproveBadgeInstructionDataEncoder(),
    getApproveBadgeInstructionDataDecoder(),
  );
}

export type ApproveBadgeAsyncInput<
  TAccountReviewer extends string = string,
  TAccountFreelancer extends string = string,
  TAccountBadge extends string = string,
> = {
  reviewer: TransactionSigner<TAccountReviewer>;
  freelancer: Address<TAccountFreelancer>;
  badge?: Address<TAccountBadge>;
  badgeIndex: ApproveBadgeInstructionDataArgs["badgeIndex"];
  clientLinkedin: ApproveBadgeInstructionDataArgs["clientLinkedin"];
  clientTwitter: ApproveBadgeInstructionDataArgs["clientTwitter"];
  clientEmailReviewer: ApproveBadgeInstructionDataArgs["clientEmailReviewer"];
};

export async function getApproveBadgeInstructionAsync<
  TAccountReviewer extends string,
  TAccountFreelancer extends string,
  TAccountBadge extends string,
  TProgramAddress extends Address = typeof REPULINK_PROGRAM_ADDRESS,
>(
  input: ApproveBadgeAsyncInput<
    TAccountReviewer,
    TAccountFreelancer,
    TAccountBadge
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ApproveBadgeInstruction<
    TProgramAddress,
    TAccountReviewer,
    TAccountFreelancer,
    TAccountBadge
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? REPULINK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reviewer: { value: input.reviewer ?? null, isWritable: true },
    freelancer: { value: input.freelancer ?? null, isWritable: false },
    badge: { value: input.badge ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.badge.value) {
    accounts.badge.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([98, 97, 100, 103, 101])),
        getAddressEncoder().encode(expectAddress(accounts.freelancer.value)),
        getU32Encoder().encode(expectSome(args.badgeIndex)),
      ],
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.reviewer),
      getAccountMeta(accounts.freelancer),
      getAccountMeta(accounts.badge),
    ],
    data: getApproveBadgeInstructionDataEncoder().encode(
      args as ApproveBadgeInstructionDataArgs,
    ),
    programAddress,
  } as ApproveBadgeInstruction<
    TProgramAddress,
    TAccountReviewer,
    TAccountFreelancer,
    TAccountBadge
  >);
}

export type ApproveBadgeInput<
  TAccountReviewer extends string = string,
  TAccountFreelancer extends string = string,
  TAccountBadge extends string = string,
> = {
  reviewer: TransactionSigner<TAccountReviewer>;
  freelancer: Address<TAccountFreelancer>;
  badge: Address<TAccountBadge>;
  badgeIndex: ApproveBadgeInstructionDataArgs["badgeIndex"];
  clientLinkedin: ApproveBadgeInstructionDataArgs["clientLinkedin"];
  clientTwitter: ApproveBadgeInstructionDataArgs["clientTwitter"];
  clientEmailReviewer: ApproveBadgeInstructionDataArgs["clientEmailReviewer"];
};

export function getApproveBadgeInstruction<
  TAccountReviewer extends string,
  TAccountFreelancer extends string,
  TAccountBadge extends string,
  TProgramAddress extends Address = typeof REPULINK_PROGRAM_ADDRESS,
>(
  input: ApproveBadgeInput<TAccountReviewer, TAccountFreelancer, TAccountBadge>,
  config?: { programAddress?: TProgramAddress },
): ApproveBadgeInstruction<
  TProgramAddress,
  TAccountReviewer,
  TAccountFreelancer,
  TAccountBadge
> {
  // Program address.
  const programAddress = config?.programAddress ?? REPULINK_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    reviewer: { value: input.reviewer ?? null, isWritable: true },
    freelancer: { value: input.freelancer ?? null, isWritable: false },
    badge: { value: input.badge ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.reviewer),
      getAccountMeta(accounts.freelancer),
      getAccountMeta(accounts.badge),
    ],
    data: getApproveBadgeInstructionDataEncoder().encode(
      args as ApproveBadgeInstructionDataArgs,
    ),
    programAddress,
  } as ApproveBadgeInstruction<
    TProgramAddress,
    TAccountReviewer,
    TAccountFreelancer,
    TAccountBadge
  >);
}

export type ParsedApproveBadgeInstruction<
  TProgram extends string = typeof REPULINK_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    reviewer: TAccountMetas[0];
    freelancer: TAccountMetas[1];
    badge: TAccountMetas[2];
  };
  data: ApproveBadgeInstructionData;
};

export function parseApproveBadgeInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedApproveBadgeInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      reviewer: getNextAccount(),
      freelancer: getNextAccount(),
      badge: getNextAccount(),
    },
    data: getApproveBadgeInstructionDataDecoder().decode(instruction.data),
  };
}
